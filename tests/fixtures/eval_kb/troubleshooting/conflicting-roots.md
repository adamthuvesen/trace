# Conflicting collection roots

Setting both `KB_PATH` and `KB_COLLECTIONS` raises an error at startup:
**`KB_PATH and KB_COLLECTIONS are mutually exclusive`**. Pick one — a single
root or named collections — and unset the other.
