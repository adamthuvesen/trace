# Linear score combination

A **linear combination** merges retrievers by adding their weighted scores into
one number. It needs the scores normalized onto the same scale and the weights
tuned by hand, in contrast to rank-based fusion.
