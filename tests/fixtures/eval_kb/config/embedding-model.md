# Embedding model

Set **`EMBEDDING_MODEL`** to `all-MiniLM-L6-v2` (the default, 384-dimension) or
`BAAI/bge-base-en-v1.5` for higher-quality embeddings at a larger index size.
Changing this value requires a forced reindex.
