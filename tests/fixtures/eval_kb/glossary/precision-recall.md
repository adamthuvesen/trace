# Precision and recall

**Precision** is the fraction of returned results that are relevant; **recall**
is the fraction of all relevant results that were returned. Tuning a retriever
usually trades one against the other.
