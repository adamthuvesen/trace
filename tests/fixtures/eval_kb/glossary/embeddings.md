# Embeddings

**Embeddings** map text into dense numeric vectors so that passages with related
meaning land near each other in the space. Retrieval then compares vectors
instead of raw words, which is what lets it match paraphrases.
