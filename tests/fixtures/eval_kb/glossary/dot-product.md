# Dot product

The **dot product** multiplies two vectors element-wise and sums the result.
Unlike the cosine measure it grows with vector magnitude, so longer vectors tend
to score higher even when their direction is similar.
