# Cosine similarity

**Cosine similarity** compares the angle between two vectors and ignores their
magnitude. It is the usual way to rank embeddings by how close their directions
point, so a short and a long vector can still match strongly.
