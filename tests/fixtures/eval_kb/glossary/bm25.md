# BM25

BM25 is a classic **keyword** ranking function for lexical retrieval. It scores
bag-of-words queries by combining term frequency saturation with document-length
normalization, and is a common baseline when comparing **ranking** quality.
