# Benchmark Artifacts

These files are committed benchmark outputs unless a specific report says it was regenerated. Treat timestamps and latency/accuracy numbers as run data, not freshly regenerated measurements.

Use new eval runs for current performance claims.
